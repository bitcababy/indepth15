%h1 Listing teacher_sections

%table
  %tr
    %th
    %th
    %th

  - @sections.each do |teacher_section|
    %tr
      %td= link_to 'Show', teacher_section
      %td= link_to 'Edit', edit_teacher_section_path(teacher_section)
      %td= link_to 'Destroy', teacher_section, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Section', new_teacher_section_path
