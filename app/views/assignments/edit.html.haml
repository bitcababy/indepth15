= simple_form_for @assignment, as: :assignment, url: teacher_course_assignment_path(@teacher, @course, @assignment), method: :put do |f|
	%tbody
		#editor= f.input :content, as: :ckeditor, input_html: { height: 600, toolbar: "Full" }, label: 'Contents'