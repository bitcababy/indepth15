%h1 Listing admin_courses

%table
  %tr
    %th
    %th
    %th

  - @courses.each do |admin_course|
    %tr
      %td= link_to 'Show', admin_course
      %td= link_to 'Edit', edit_admin_course_path(admin_course)
      %td= link_to 'Destroy', admin_course, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Course', new_admin_course_path
