#sections_pane
	#info
		= "Course #{@course.number} — #{duration_string(@course.duration)} — #{@course.credits} Credits"
	#description
		!= @course.description
	#leadin
		= "In the #{academic_year_string(Settings.academic_year)} academic year there #{is_are_number_mangler(@course.sections.current.count, 'section')} of #{@course.full_name}."

	%div= render partial: 'courses/sections_table', obj: @course
