#sections_pane
	#info
		= "Course #{@course.number} — #{@course.duration} — #{@course.credits} Credits"
	#description
		= @course.description
	#leadin
		= "In the #{academic_year_string} academic year there #{is_are_number_mangler(@course.current_sections.count, 'section')} of #{@course.full_name}."

	%div= render partial: 'courses/sections_table', obj: @course
