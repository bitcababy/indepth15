!!!
%html
	%head
		%title= content_for?(:title) ? (yield :title) : "The titel"
		= stylesheet_link_tag		 "application", :media => "all"
		= javascript_include_tag "application"
		= csrf_meta_tags
	%body
		#page_header= content_for?(:page_header) ? (yield :page_header) : "The header"
		= yield
