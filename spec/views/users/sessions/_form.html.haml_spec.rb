require 'spec_helper'

describe '/users/sessions/new' do
	it "renders a sign-in form" do
		# render
		# rendered.should have_selector('form', action: '/users/sign_in') do |f|
		# 	f.should have_selector('input', name: 'user[login]')
		# 	f.should have_selector('input', name: 'user[password]')
		# 	f.should have_selector('input', name: 'user[remember_me]')
		# 	f.should have_selector('div.form-actions') do |fa|
		# 		fa.should have_selector('input', name: 'commit', type: 'submit')
		# 	end
		# end
	end
			
end
