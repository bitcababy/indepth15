require 'spec_helper'

describe '/users/sessions/new' do
	it "renders a sign-in form" do
		# render
		# expect(rendered).to have_selector('form', action: '/users/sign_in') do |f|
		# 	expect(f).to have_selector('input', name: 'user[login]')
		# 	expect(f).to have_selector('input', name: 'user[password]')
		# 	expect(f).to have_selector('input', name: 'user[remember_me]')
		# 	expect(f).to have_selector('div.form-actions') do |fa|
		# 		expect(fa).to have_selector('input', name: 'commit', type: 'submit')
		# 	end
		# end
	end
			
end
