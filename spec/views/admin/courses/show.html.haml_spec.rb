require 'spec_helper'

describe "admin/courses/show" do
  before(:each) do
    @course = assign(:course, mock('course'))
  end

  it "renders attributes in <p>" do
		pending "Unfinished test"
    render
    # Run the generator again with the --webrat flag if you want to use webrat matchers
  end
end
