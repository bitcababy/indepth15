require 'spec_helper'

describe Document::CoursePage do
	# it { should belong_to(:course) }
	# 
	# it "should be able to change its contents" do
	# 	expect {
	# 		subject.content = "foo"
	# 	}.to change{subject.content}.to("foo")
	# end
	# 
	# context "scopes" do
	# 	describe "of_kind" do
	# 		it "should return all course_pages of a given kind" do
	# 			3.times {Fabricate :course_page, kind: :foo}
	# 			2.times {Fabricate :course_page, kind: :bar}
	# 			CoursePage.of_kind(:foo).count.should == 3
	# 		end
	# 	end
	# 	
	# 	describe "for_course" do
	# 		it "should return all course_pages for a given course" do
	# 			course1 = Fabricate :course
	# 			course2 = Fabricate :course
	# 			# 3.times {Fabricate :course_page, course: course1 }
	# 			# 2.times {Fabricate :course_page, course: course2 }
	# 			3.times {course1.course_pages.push Fabricate(:course_page)}
	# 			2.times {course2.course_pages << Fabricate(:course_page)}
	# 			CoursePage.count.should == 5
	# 			CoursePage.for_course(course1).count.should == 3
	# 		end
	# 	end
	# 	
	# end # scopes
	
end